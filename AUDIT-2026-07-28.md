# Reanálise do código gerado — 2026-07-28

**Escopo:** varredura sistemática do que foi produzido pelas iterações
recentes (agenda de defesas, cards de publicação, a11y mobile, LGPD/GA
opt-out, migrações de dados de defesas). Cobre `layouts/`, `scripts/`,
`assets/`, `data/`, `content/`, `schemas/` e `hugo.yaml`.

**Método:** 4 agentes de exploração em paralelo (Explore, thoroughness
"very thorough" para os 3 primeiros; "medium" para o de dados).

**Estado final:** 8 das 8 ações propostas foram executadas neste ciclo.
Detalhes por seção abaixo.

---

## 1. Achados por severidade

### 🔴 Crítico
| # | Achado | Local | Ação tomada |
|---|---|---|---|
| C1 | Arquivo órfão carregado no build | `layouts/index copy.html.orig` (17 KB) | **Removido** |
| C2 | Falta de schema formal para defesas | `schemas/` (9 schemas, sem `defesa`) | **Criado** `schemas/defesa.schema.json` |
| C3 | 132 defesas referenciam advisor slug ausente em `data/people.yaml` | ex.: `sergio_freitas` só existe em `content/people/` | **Documentado** — arquitetura split intencional; validador não emite erro pois `collect_advisor_page_ids` já lê de `content/people/` |
| C4 | Página em branco em `defesa_id` inexistente | `layouts/defesas/single.html:11-13` | Sinalizado — resolver em ciclo futuro (fallback visual) |
| ~~C5~~ | ~~4 defesas TCC1 sem `production_id`~~ | ~~`tcc1-ian-christian-2026` e 3 outras~~ | **Falso positivo (2026-07-28)**: TCC1, qualification (mestrado) e phd_qualification (doutorado) são defesas de proposta/planejamento e por design **não** têm produção catalogável — `production_id: null` é o valor correto. Documentado em `schemas/defesa.schema.json` |

### 🟠 Alto
| # | Achado | Local | Ação tomada |
|---|---|---|---|
| A1 | Lógica de estado (`upcoming\|held\|deposited\|withdrawn`) triplicada | `defesas/single.html:15-29`, `defesa-body.html:23-37`, `defesas/list.html:13-42` | **Extraído** `partials/defesa-state.html` |
| A2 | Conversão de mês PT (12 `replace`) duplicada | `defesa-body.html:65-81` + `defesas/single.html:44-60` | **Extraído** `partials/defesa-date-pt.html` |
| A3 | i18n hardcoded via `cond $isPortuguese` | `layouts/index.html:4-31` (20+ strings), `defesas/single.html`, index news label | Sinalizado — próximo ciclo |
| A4 | N+1 em templates de defesa | `defesa-body.html:216-227, 235-256` | Sinalizado — pré-calcular no build |
| A5 | Target-size 40×40 no theme-toggle (WCAG 2.5.5 exige 44) | `partials/theme-toggle.html:27` (`min-h-10 min-w-10`) | **Corrigido** para `min-h-11 min-w-11` |
| A6 | Marquee sem pausa por teclado | `layouts/index.html:745` | Sinalizado — WCAG 2.2.2 |
| A7 | 95 `!important` + 258 refs `#profile-content` no CSS | `assets/css/profile.css` | Sinalizado — Fase 1 do PLANO-DESIGN |
| A8 | Scripts históricos ativos em `scripts/` | `migrate_advisors_to_frontmatter.py`, `create_external_advisor_stubs.py` | **Arquivados** em `docs-src/historical/` |
| A9 | `parse_frontmatter` duplicado em 6+ scripts | ver `scripts/` | **Centralizado** em `scripts/shared/frontmatter.py` — `validate_content.py` e `validate_i18n.py` refatorados |
| A10 | Slugify em 3 variantes divergentes | `normalize_people_slugs.py`, `apply_people_slugs.py`, `build_publications.py` (`gen_defesa_slug.py` é caso à parte) | **Centralizado** núcleo (`strip_accents`, `slugify_snake`) em `scripts/shared/slugify.py`. Refatoração progressiva dos consumers a fazer no próximo toque em cada script |

### 🟡 Médio
| # | Achado | Local | Nota |
|---|---|---|---|
| M1 | Regressão a11y falso-positivo — transições em `profile.css:577, 599, 603` | supostamente fora de `prefers-reduced-motion` | **Verificado**: `main.css:171-182` tem regra global com seletor universal `*, *::before, *::after` que já cobre. Nenhuma alteração necessária. |
| M2 | `params.color: blue`, `disable_stay_uptodate` órfãos | `hugo.yaml:18/294, 27/303` | Sinalizado — limpeza opcional |
| M3 | GA opt-out sem `aria-live` no widget | `privacy.googleAnalytics.disable: false` | Sinalizado |
| M4 | Rerun de `migrate_defesas.py` pode zerar `defesa_id` fora de escopo | `scripts/migrate_defesas.py:753-759` | Sinalizado — script marcado idempotente |
| M5 | Exit code 0 em erro fatal nos `migrate_*.py` | vários | Sinalizado |
| M6 | Regex com backtracking em `_parse_banca()` | `migrate_defesas.py:441-451` | Sinalizado — risco baixo, dados internos |
| ~~M7~~ | ~~`content/projects/*.md` com campos obrigatórios ausentes~~ | ~~ex.: `doarti.pt.md` sem `partners`, `funding_agencies`~~ | **Falso positivo (2026-07-28)**: varredura em todos os 42 projetos confirmou que todos os 12 campos obrigatórios do schema estão presentes (listas vazias `[]` incluídas). `validate_content.py` também não emite warnings sobre `content/projects/`. |

### 🟢 Baixo / housekeeping
- `Counter` importado sem uso em `migrate_defesas.py:35`
- `OrderedDict` mantido para compat Python < 3.7 em `consolidate_supervisions.py:52`
- `apply_profile_level.py:147` sugere flag `--apply` obsoleta
- Split anômalo pessoas: só 6 no `data/people.yaml` **e** em `content/people/`

---

## 2. Mudanças aplicadas neste ciclo

### Arquivos removidos
- `layouts/index copy.html.orig` (17 KB, órfão)

### Arquivos movidos
- `scripts/migrate_advisors_to_frontmatter.py` → `docs-src/historical/`
- `scripts/create_external_advisor_stubs.py` → `docs-src/historical/`

### Arquivos criados
- `schemas/defesa.schema.json` — schema JSON Draft 2020-12 para as 151 defesas em `data/defesas.yaml`
- `layouts/partials/defesa-state.html` — resolve estado derivado
- `layouts/partials/defesa-date-pt.html` — formata `time.Time` como "N de mês de AAAA" em PT
- `scripts/shared/__init__.py`
- `scripts/shared/frontmatter.py` — `parse_frontmatter(path) → (fm, body)`
- `scripts/shared/slugify.py` — `strip_accents()`, `slugify_snake()`

### Arquivos editados
- `layouts/partials/theme-toggle.html` — `min-h-10 min-w-10` → `min-h-11 min-w-11` (WCAG 2.5.5)
- `layouts/defesas/single.html` — usa novos partials; -30 linhas
- `layouts/partials/defesa-body.html` — usa novos partials; -22 linhas
- `layouts/defesas/list.html` — usa novo partial de estado; -8 linhas
- `scripts/validate_content.py` — nova função `validate_defesas` chama `validate_against_schema` com `defesa.schema.json`; `parse_frontmatter` delega para `shared.frontmatter`
- `scripts/validate_i18n.py` — `parse_frontmatter` delega para `shared.frontmatter`
- `docs-src/data-model.md` — linha 273 atualizada (script arquivado)

---

## 3. Verificação

| Passo | Resultado |
|---|---|
| `python3 scripts/validate_content.py` | 0 erros, 29 warnings (todos pré-existentes: defesas legadas em `productions.yaml` sem `defesa_id`, fora do escopo do modelo novo) |
| `python3 scripts/validate_i18n.py` | 0 erros |
| `hugo --minify` | 1160 páginas PT + 1154 EN geradas em 10.5 s, sem erros |

O schema recém-criado valida as 151 defesas com **0 violações**.

---

## 4. Pendências para próximos ciclos

Ordem sugerida (impacto × esforço):

1. ~~Fallback visual em `defesas/single.html:11-13`~~ — **feito 2026-07-28** (tela amigável com links para /defesas/ e /contact/, envolvida em `if/else` porque `return` só vale em partials).
2. ~~Completar 4 TCC1 sem `production_id`~~ — **não se aplica**: TCC1/qualification/phd_qualification não geram produção catalogável por design.
3. ~~Extrair i18n hardcoded~~ — **feito 2026-07-28**: 22 chaves `ui_home_*` adicionadas em `i18n/{pt,en}.yaml`; `layouts/index.html:4-35` refatorado; URLs simplificadas via `relLangURL` para paths compartilhados e `ui_home_url_join` para o único path que difere (`junte-se/` vs `join/`).
4. ~~Pré-calcular `year` + N+1 em defesa-body~~ — **feito 2026-07-28**: (a) `layouts/index.html:67-71` — 329 items em `productions.items` agora usam `int .year` direto (todos verificados como int), eliminando `printf`/`findRE`/`replaceRE` por item; (b) `layouts/partials/defesa-body.html:189-208` — mapas `$productPagesById` e `$publicationPagesByKey` pré-computados uma vez, transformando O(N×M) em O(M+N). Verificado que "Git Ranking" ainda renderiza na defesa `ana-beatriz-2026-07-14`.
5. ~~Validar `content/projects/*.md` contra schema~~ — **verificado 2026-07-28**: 42/42 projetos com todos os 12 campos obrigatórios.
6. ~~Refatoração progressiva dos consumers~~ — **feito 2026-07-28**: `build_publications.py` delega `parse_frontmatter` a `shared`; `normalize_people_slugs.py` reexporta `slugify`/`_strip_accents` de `shared.slugify`; `apply_people_slugs.py` importa `slugify`/`strip_accents` do shared e reusa em `short_slug`. `derive_people.py` continua funcionando via reexport (sem mudança). `consolidate_collaborators.split_frontmatter` mantido — assinatura incompatível (retorna string YAML preservando formato).
7. ~~A11y: pausa por teclado no marquee da home~~ — **feito 2026-07-28**: `layouts/index.html:740-780` recebeu `x-data="{ paused: false }"`, `group-focus-within:[animation-play-state:paused]`, botão de toggle com `min-h-11 min-w-11` e `aria-pressed` bilíngue via `i18n "ui_partners_marquee_pause/resume"`. CSS gerado inclui `.group-focus-within\:...:is(:where(.group):focus-within *){animation-play-state:paused}`.
8. ~~CSS Fase 1 — !important em profile.css~~ — **feito 2026-07-28**: verificado que **nenhuma** das 17 páginas de perfil renderizadas tem `.prose` como ancestral de `#profile-content` (todos os blocos internos já usam `not-prose`). Os 129 `!important` defensivos foram todos removidos via `sed`. CSS bundle: 218 KB → 208 KB (-5%). 259 refs `#profile-content` mantidas (regras funcionam por especificidade normal do id). Redução de profundidade de seletores fica para Fase 2 (requer reestruturação de HTML).
9. ~~Housekeeping~~ — **feito 2026-07-28**: (a) removidos `params.color: blue` e `disable_stay_uptodate: true` de PT e EN em `hugo.yaml` (0 consumidores confirmados); (b) `Counter` removido de `migrate_defesas.py:36`; (c) mensagem `--apply` em `apply_profile_level.py:147` **mantida** — reanálise mostrou que a flag ainda é canônica do script; (d) `OrderedDict` em `consolidate_supervisions.py` **mantido** — usado ativamente em 6 pontos como representer YAML, não é só compat.

~~**Migração histórica pendente (não estava no AUDIT original):** 29 itens em `data/productions.yaml` com `type ∈ {tcc, dissertation, specialization}` e ano ≥ 2008 não têm `defesa_id` — são defesas pré-refactor ainda não migradas para `data/defesas.yaml`. Cada item aparece como `[WARNING] defesas :: data/productions.yaml::[N]` na saída do validador.~~

**Resolvido 2026-07-28 (Opção C — híbrida):**
- **5 TCCs recentes (2020-2023)** — todos do IFB (curso_tsi_ifb), orientados por Daniel Sundfeld — migrados via script one-shot. Novas entradas em `data/defesas.yaml`: `francisca-mariana-2023-06-15`, `almir-guilherme-2022-06-15`, `levi-junior-2021-06-15`, `rodrigo-gomes-2020-06-15`, `eneias-oliveira-2020-06-15` (com `date_approximate: true` + `needs_manual_review: true` porque só temos ano; banca vazia porque não recuperada). Cada item correspondente em `data/productions.yaml` recebeu `defesa_id`.
- **24 especializações antigas (2008-2010)** — marcadas com `historical_no_defense_record: true` em `data/productions.yaml`. `scripts/validate_content.py` foi ajustado para reconhecer o flag e não emitir warning. Convenção documentada em `CONVENTIONS.md §2.8.1`.
- **Resultado:** `validate_content.py` agora reporta `0 erros, 0 warnings`. `data/defesas.yaml` cresceu de 151 → 156 entradas.
